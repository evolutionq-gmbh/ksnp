Bindings for KSTP
=================

This crates provides bindings for the KSTP library. For safe wrappers, see the
`kstp` crate.

The bindings are generated using `bindgen`, the library itself is build with
CMake using the build script.
